// hoisting means we can use a variable before it is declared.

//variable and function are hoisted which means their declaration is moved on top of the code.




console.log(score); // undefined

var score = 7;  