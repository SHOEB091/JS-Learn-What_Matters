/*functions are mainly used tp perform three task 
1. when we want to resue the task 
2. when we want to perform a task that is complex devide code in parts 
3. when we want to perform a task that is repetitive 
*/

// function declaration

/*
function functionName(Parameter1, Parameter2, ...)
{
    //function body
}

*/

function myFunction (g1, g2) {
    return g1 + g2;
}
const value = myFunction(2, 3);
console.log(value);